[project]
name = "prepaired"
version = "0.2.0"
description = "Adaptive AI interview preparation platform with multi-agent orchestration and RL-guided strategy"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.pyright]
pythonVersion = "3.12"
include = ["agents", "apps/backend", "services"]
