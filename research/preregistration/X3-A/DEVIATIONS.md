# X3-A deviations log

| # | Date | Event | Classification | Action |
|---|---|---|---|---|
| 1 | 2026-09-19 | First confirmatory launch (`results_attempt1_ABORTED_process_killed/`): gate G-HARNESS passed (9/9, `gate_G-HARNESS.csv`) and the manifest was written (`status: started`), but the process was terminated before `sessions.csv` was written (the launcher shell that hosted the background process exited). No session data, no result and no analysis output exist for this attempt. | Infrastructure interruption; not a scientific finding; no data seen. | Attempt retained unmodified; the run was relaunched with identical code, config, protocol tag and lock as a supervised background task (`results/`). No parameter changed. |
| 2 | 2026-09-19 (correction of entry 1) | **Entry 1 was mistaken.** The first launch was **not** killed: its background process kept running and finished. I had already moved its `results/` directory to `results_attempt1_ABORTED_process_killed/` (that directory therefore holds only its early gate output, manifest stub and env log) and relaunched a second identical process, whose Manifest recreated `results/`. The first process then wrote `sessions.csv` and `personas.csv` into the recreated `results/` and finished its manifest (`manifest_X3-A-run.json`, `started_utc` of the first launch, `status: completed`, outputs hashed at its finish); the second process wrote its own `gate_G-HARNESS.csv` and then aborted with `FileExistsError` when writing `sessions.csv` (write-once protection worked). | Process-management error by the executor (two concurrent launches); no scientific effect: code, config, protocol tag, lock and seeds were identical and the run is deterministic. | The complete `results/` is used for analysis. Provenance: `sessions.csv`/`personas.csv`/manifest from launch 1, `gate_G-HARNESS.csv` from launch 2 (both gates 9/9). The directory name `results_attempt1_ABORTED_process_killed/` is retained as written (misnomer noted here). |
