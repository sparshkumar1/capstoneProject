# P0 Forensic Summary — final triage (2026-09-19)

Scope: resolution pass on the seven P0 issues from `CLAUDE_FULL_RESEARCH_AUDIT.md`. **No fix was implemented, no experiment or test was rerun, no frozen artifact was edited, no manuscript text was written.** The only files created are the eight `research/audit/P0_*` files. The Paper 3 numbers below come from the frozen files and from an evaluation-only replay of the frozen checkpoints (plus control policies run through the frozen simulator; those are audit diagnostics, not frozen evidence).

Detail files: `P0_1_CROSSENCODER_FORENSIC.md`, `P0_2_PPO_CONTRIBUTION_FORENSIC.md`, `P0_3_BOUNDARY_FORENSIC.md`, `P0_4_GUARDRAIL_FORENSIC.md`, `P0_5_VOLATILITY_FORENSIC.md`, `P0_6_PAPER1_EVIDENCE_FORENSIC.md`, `P0_7_HUMAN_ETHICS_FORENSIC.md`.

Classification key: **DOCUMENTATION_ERROR** (statement wrong or unsupported; the evidence is fine) · **ANALYSIS_ERROR** (numbers or comparison mis-analysed/misdescribed; correct numbers exist) · **EVIDENCE_GAP** (needed evidence does not exist or is unrecoverable) · **CONFIRMED** (the audit's finding is true as stated). A P0 may carry more than one.

## 1. Triage table

| P0 | Problem | Classification | Current supported claim | Unsupported claim | Fix (recommendation only) | Rerun required? | User decision required? |
|---|---|---|---|---|---|---|---|
| **P0-1** | CrossEncoder `tuned_model2` is described as "off-the-shelf / zero fine-tuning" | **CONFIRMED** (weights refute it) · **EVIDENCE_GAP** (training data, code, hyperparameters unrecoverable) · **DOCUMENTATION_ERROR** ("off-the-shelf" in audits/handoff) | R is computed by a checkpoint derived from `cross-encoder/ms-marco-MiniLM-L-6-v2`: embeddings and layers 0–3 bit-identical to upstream; layers 4–5, pooler, classifier changed (16.28 % of parameters); SHA-256 `6a241a55…`; no upstream revision matches. Paper 2 validates this exact frozen file as a black box. Two fine-tune runs existed (`1_best_model_zip`: 1245 steps, val ρ 0.7918, deleted 2026-08-29; `tuned_model2`: 660 steps, val ρ 0.785). | "off-the-shelf", "zero-shot", "no fine-tuning / domain adaptation"; "trained on interview/passage-ranking pairs"; "12-layer L12"; any train/test-independence or leakage-free claim; `val_spearman 0.785` as performance | Ask whoever trained it for notebook, data, split, hyperparameters (both runs); write a new provenance/errata file; disclose as "derived, fine-tuning undocumented". | **No** for reframing. **Optional new evaluation**: run Paper 2's benchmark with the upstream checkpoint (`821d1aa6…`) to bound the effect, if training provenance cannot be recovered. No retraining. | **Yes** — who trained it; disclose vs. robustness run vs. drop claim |
| **P0-2** | Paper 3's gain/stability attributed to PPO | **ANALYSIS_ERROR** (attribution) · **CONFIRMED** (PPO contribution not identified) | Guardrail rules alone cut MAE 1.200 → ~0.67; constant-Same + guardrails = 0.673 vs PPO + guardrails 0.673–0.687 (5 of 125 sessions differ; pooled ΔMAE +0.0044, PPO marginally worse). Raw PPO gives a modest, seed-dependent gain over fixed (0.958 vs 1.200; 0.804–1.138) only for low-skill personas. Heuristic is better than both (0.473). | "PPO learns adaptive difficulty"; "PPO+guardrails improves tracking because of PPO"; "0.006 seed SD shows stable PPO"; PPO superiority over anything but fixed | Reframe as "guardrail-controlled policy, with raw PPO and heuristic as comparators and constant-Same+G as a labelled control"; addendum for the frozen report. | **No** to reframe. **Yes (new experiment)** if a PPO-specific claim is kept (more personas, guardrail-only and random+G controls in the design, training-consistent env, persona as unit) | **Yes** — reframe vs. new experiment |
| **P0-3** | "0 violations / eliminates 100 % of out-of-bounds transitions" | **CONFIRMED** (false as stated) · **ANALYSIS_ERROR** (hard-coded 0 overriding computed 232; attempts mislabelled) · **DOCUMENTATION_ERROR** (guardrails docstring advertises an unimplemented boundary clamp) | The simulator/orchestrator clamps difficulty to [1,5] for every policy, so actual post-clip states are always in range (an invariant, not a result). Attempted out-of-range actions: raw PPO 136/1,250 turns (10.9 %), guarded 232/1,250 (18.6 %); all at the floor; frozen `paper3_seed_results.csv` already lists 26/71/41/33/61. | "0 constraint violations"; "eliminates 100 % of OOB"; guardrails enforce boundaries (module has no boundary logic; they increased attempts) | Errata with real counts and a renamed metric ("boundary-saturated actions"); report activations/overrides separately (P0-4); optional code/doc cleanup later. | **No** | **Yes** — wording and metric choice |
| **P0-4** | "563 interventions on 45 % of turns to rescue struggling candidates" | **ANALYSIS_ERROR / DOCUMENTATION_ERROR** — the numbers are correct; their description is wrong | 563 rule *activations* (45.0 % of 1,250 turns); **99 action changes (7.9 %)**; 464 activations (82.4 %) left PPO's action unchanged; per-seed change rate 4.8–14.8 %; 41/125 sessions had ≥1 change; all changes in two personas; 64 of 99 lowered difficulty, 30 blocked a decrease, 5 blocked a "Harder" into "Same". G6 never fires. | "intervened on 45 % of turns"; "rescued struggling candidates" as the description of all 563; "guardrails only make it easier" | Report activation rate, override rate, no-op share, session-level rate, per rule/persona, as separate measures (wording proposed in P0-4 §5); errata; correct the description of `paper3_guardrail_results.csv` (213 rows, not 563). | **No** | **Yes** — wording only |
| **P0-5** | "PPO has lower volatility than the heuristic (0.088 vs 0.160)" | **ANALYSIS_ERROR** | 0.088 is the **seed-123-only** value. Guarded PPO over 5 seeds: 0.186 (0.088–0.268) vs heuristic 0.160; 2/5 seeds lower; pooled paired difference +0.026 [−0.024, +0.078] (no reliable difference). Raw PPO 0.078 → guarded 0.186 (frozen `volatility_reduction_pct = −140.21`). Low volatility largely = not adapting for high-skill personas (const-Same+G gives 0.080). | "PPO is smoother/less volatile than the heuristic"; "guardrails reduce volatility"; seed 123 as representative (no primary-seed rationale in the frozen config) | Replace with the five-seed trade-off table (MAE, volatility, oscillation) in an addendum; keep frozen values as historical record. | **No** | **Yes** — wording |
| **P0-6** | Paper 1 fault (10/10), Qwen (5/5) and test-count results | **CONFIRMED** (fault and Qwen rows are literal PASS) · **EVIDENCE_GAP** · **DOCUMENTATION_ERROR** | Real: 9 adversarial programs actually run (4 outcomes match the intended control, 5 non-discriminating); SQLite concurrency to 25 sessions (0 errors, synthetic ops); latency measurements (mean contaminated by cold start); `ScoreValidator` handling (4 executed asserts + unit tests); mocked-failure tests exist for evaluator/Qwen/Docker/empty-transcript. Fault: A×1, B×5, C×4. Qwen: B×3, C×2. | "10/10 faults recovered"; "fault-tolerant"; "5/5 Qwen boundaries verified" (`FeedbackValidator` does not exist); "9/9 contained"; "213 passed, 0 failed" (no log; only `204 passed, 1 skipped` recorded) | Reword to the supportable statements in P0-6 §6; **or** build a real fault-injection / Qwen-isolation / security-oracle campaign with pre-specified criteria and stored logs (P0-6 §7). | **Only if the claims stay**: new experiments (not a rerun of the frozen script — that would re-emit the same literal PASS). | **Yes** — reword vs. build the campaign |
| **P0-7** | Human ethics / rater provenance documentation | **EVIDENCE_GAP** (rater/adjudicator provenance, consent, compensation) · **DOCUMENTATION_ERROR** ("independent / real / committee" wording) · institutional status **unknown** | Scientific reproducibility of the human gold is strong (hash-pinned files; ICC/α reproduce). Timeline and file evidence documented; no PII in the repo. | Rater independence, qualifications, "expert committee" adjudication, voluntary informed consent, compensation terms, any ethics approval/exemption/"not human-subjects" determination; "frozen before annotation" if packages were sent before 13:59 | Obtain the records from the people involved and the institution's position; add a NEW addendum (do not edit the frozen checklist/manifests); if none exist, disclose and reword, or collect a documented new rating round. | **No analysis rerun.** New human data collection only if provenance cannot be documented. | **Yes — user and institutional.** Approval/exemption status is absent from the repository and is a publication/documentation risk |

## 2. Which P0s need what

**Need new experiments only if the corresponding claim is to be kept**
- P0-2 (PPO-specific claim): new Paper 3-style study with the controls listed above.
- P0-6 (fault tolerance, Qwen isolation, security containment, test counts): new fault-injection / isolation / security-oracle tests with logged results, and a stored pytest report.
- P0-1 (optional): an evaluation-only robustness run of Paper 2 with the verified upstream checkpoint — needed only if the fine-tuning provenance cannot be recovered and a leakage-related claim is wanted.
- P0-7 (only if provenance cannot be documented): a new, documented human rating round.

**Documentation / analysis corrections only (no rerun; correct numbers already exist in frozen files or the replay)**
- P0-3, P0-4, P0-5 entirely.
- P0-1, P0-2, P0-6, P0-7 for the reframing/wording option.

**Need a user and/or institutional decision or action**
- P0-1: who trained the model and where the artifacts are.
- P0-2: reframe Paper 3 vs. commission a new experiment.
- P0-6: reword Paper 1 vs. build the campaign.
- P0-7: rater/adjudicator records; consent and compensation records; institutional determination (unresolved, absent from the repository).
- P0-3, P0-4, P0-5: approve the replacement wording/metrics.

## 3. Findings added by this pass that the first audit did not have

1. A second fine-tuned checkpoint (`1_best_model_zip`) existed and was removed on 2026-08-29; the repository never says which run is "the" model or why (P0-1).
2. Every published upstream revision of the MS-MARCO L6 weights has the same hash (`821d1aa6…`); none matches the tuned file (P0-1).
3. `best_model_info.json` is a real Huber+rank fine-tune record; the audit document explaining it as an "upstream MS-MARCO record" has no support (P0-1).
4. The frozen files already contain the correct, unfavourable numbers (232 attempted violations in `paper3_seed_results.csv`; `volatility_reduction_pct = −140.21` in `paper3_raw_results.json`); the reports and handoff used hard-coded zeros and the seed-123 value (P0-3, P0-5).
5. `paper3_guardrail_results.csv` holds 213 rows (seed 123 + historical-mismatch), not the "563 activations" its freeze description says (P0-4).
6. PPO+guardrails equals fixed on both high-skill personas in every seed; the whole gain sits in two low-skill personas where the rules act (P0-2).
7. SEC-09's pass test is vacuous, and the `FeedbackValidator` cited in QWN-04 does not exist (P0-6).
8. No record of Human Gate 1 approval or a signed ethics checklist exists; rater files appear 22–53 minutes after the freeze commit; author-assigned categories explain ≈ 98–99 % of each rater's score variance (P0-7).

No statement in the first audit needed retraction; several numbers were refined (e.g. the attempted-violation and seed volatility counts were re-derived and match).

## 4. Integrity of the frozen artifacts during this pass

No file outside `research/audit/` was created or modified in the repository. Analysis scripts, extracted historical blobs and replay outputs are in the session scratchpad only (they are not part of the repository, so anything derived from them, including the control-policy numbers, would need to be regenerated by a saved script if the user wants them archived).
