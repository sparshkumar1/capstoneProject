# Final Tool / Runtime Readiness Check (read-only) — 2026-09-19

**Nature.** Read-only availability check before the autonomous research sprint. Nothing was modified, installed, repaired or run: no code or dependency change, no `.venv` change, no MCP configuration change, no experiment or study script, Phase 1 not started. Python probes used `python -B` with `PYTHONDONTWRITEBYTECODE=1`, `HF_HUB_OFFLINE=1`, `WANDB_MODE=disabled` (no bytecode written, no network use by the probes). No credential or token was printed or read into the output (for W&B only the *presence* of an entry was tested).

**Classes:** READY · READY WITH FALLBACK · NOT AVAILABLE · NOT REQUIRED NOW.

## Tool matrix
| # | Tool | Available | Health (what was checked) | Intended use in sprint | Blocking? | Action | Class |
|---|---|---|---|---|---|---|---|
| 1 | **Serena** | Yes | Responsive: `list_memories` returned (empty; no stored memories) and `get_symbols_overview("rl/guardrails.py")` returned the symbols (`apply_canonical_guardrails`, `ACTION_*`). No repair attempted | Semantic navigation of `rl/`, `services/evaluator/app.py`, frozen scripts (read-only) | No (Read/Grep would be the fallback) | None | **READY** |
| 2 | **Context7** | Yes | `resolve-library-id` found `/dlr-rm/stable-baselines3` (High reputation); `query-docs` answered the seed / `evaluate_policy` question (PPO signature has `seed`; reproducibility not guaranteed across PyTorch releases/platforms; pass `seed` at model creation and seed the env; `deterministic=True` for evaluation) | Verify SB3/Gymnasium/W&B API details when writing new replay/manifest code | No | None | **READY** |
| 3 | **Superpowers** | Yes | Skills are listed as available in this session (brainstorming, systematic-debugging, verification-before-completion, writing-plans, etc.); not invoked | Multi-step execution discipline (verification-before-completion at each Phase-1 step) | No | None | **READY** |
| 4 | **claude-md-management** | Yes | Skills `claude-md-management:revise-claude-md` / `claude-md-improver` listed; not invoked | Only for approved `CLAUDE.md` updates (e.g., N1/N2 facts, pointers) | No | Use only with user approval | **READY** |
| 5 | **Zotero MCP** | Yes (read) | `zotero_list_libraries` and `zotero_get_recent` succeeded (read-only). **The library is effectively empty**: "No items found"; one library entry with 1 item listed | Not needed in Phase 1 (no citations required). Later: verified references only | No for Phase 1. **Blocks any citation-dependent manuscript step until references are added by the user** (project rule: do not populate Zotero) | None now; user to add sources later | **READY** (read access verified; NOT REQUIRED NOW) |
| 6 | **Git** | Yes | git 2.55.0; branch `workspace/human-eval-clean-push`, HEAD `8641adf`, 6 tags; `git status` readable (39 entries = pre-existing + Phase-0/preflight files) | Status, hashes, tags, diff stats; protocol tags in Phase 2 | No | None | **READY** |
| 7 | **Docker** | Client only | Docker CLI 29.7.2 present, Docker Desktop installer files present, WSL distros `docker-desktop` and `Ubuntu-22.04` registered, **but the daemon is not running**: `docker version/info` fail to connect to `npipe:////./pipe/dockerDesktopLinuxEngine` and no Docker process is running. Presence of the sandbox image `prepaired-c-sandbox:latest` **could not be checked** | Paper 1 X1-A/C (fault injection, sandbox attacks) only | **YES — Paper 1 EXECUTION (X1) is BLOCKED.** Not blocking X3-0, X2-A or the Paper-1 stored-data analysis | User must start Docker Desktop; then re-run `docker info` and `docker image ls prepaired-c-sandbox` (not done here per "do not repair") | **NOT AVAILABLE** |
| 8 | **Python environment** | Yes | `.venv` Python 3.12.7 (Anaconda base), Windows-11-10.0.26200. Effective import versions (resolves the duplicate dist-info ambiguity): numpy 2.5.2, pandas 2.3.3, scipy 1.17.1, scikit-learn 1.8.0, torch 2.11.0+cpu, transformers 4.44.2, sentence-transformers 2.7.0, faiss 1.13.2. Known pin violations unchanged (numpy `<2`, torch `<2.7`, accelerate `<1.0`); environment is **unlocked** | All Phase-1 analyses (read-only use, gated per the preflight memo §1.3) | No | Record versions in each manifest; lock in Phase 2 | **READY** |
| 9 | **pytest / JUnit** | Yes | pytest 9.0.3; the built-in `junitxml` plugin imports and `--junitxml` appears in `--help` (no test run) | X1-D test report (later); optional test sanity checks | No | None (known failing test to be disclosed, per X1 draft) | **READY** (NOT REQUIRED NOW for Phase 1) |
| 10 | **Stable-Baselines3** | Yes | Imports; version 2.7.1 (PPO/VecNormalize/`WandbCallback`-compatible) | X3-0c replay (load frozen checkpoints); X3-B training if triggered | No. **X3-B is not blocked by SB3** (it remains blocked by approval/trigger and the N2 seeding decision, not by tooling) | None | **READY** |
| 11 | **Gymnasium** | Yes | Imports; version 0.29.1 (within the `<1.0` pin) | Environment for replay/training | No | None | **READY** |
| 12 | **W&B** | Yes (package) | `wandb` 0.30.0; `wandb.integration.sb3.WandbCallback` imports; local authentication: a `~/_netrc` entry for `api.wandb.ai` is **present** (validity not tested online; no network call made); `WANDB_API_KEY` env var not set; `.env.example` defines `WANDB_ENTITY/MODE/PROJECT` names | Not used in Phase 1 (replay runs with `WANDB_MODE=disabled`). Only for new X3-B runs, if triggered | No. If W&B were unavailable, X3-B would still be possible only under the manifest-first rule: `RUN_MANIFEST_SPEC.md` has `wandb.used=false` and `ENVIRONMENT_LOCK_SPEC.md` states W&B does not replace the manifest; W&B is therefore not mandatory in the current drafts (protocol drafts are not yet tagged; confirm at Phase 2) | None | **READY** (NOT REQUIRED NOW) |
| 13 | **Codex** (independent code audit, later) | Unconfirmed | `~/.codex/` exists (config, cache, auth file — not opened); no `codex` executable found on PATH. Cannot confirm a usable CLI/app from this session | Later independent audit of any new code (helper, replay script) | No | User to confirm Codex access before the audit stage | **NOT REQUIRED NOW** |
| 14 | **Antigravity** (optional parallel review, later) | Yes (installed) | `Antigravity` and `Antigravity IDE` under `AppData\Local\Programs`; `~/.antigravity` present; not launched | Optional parallel review on a separate branch/worktree (never the same files at once) | No | None | **NOT REQUIRED NOW** |
| 15 | **Gemini / NotebookLM** | External | `~/.gemini/` directory exists; no session connection to either | Large-context triage / literature synthesis later; not required for overnight execution | No | None | **NOT REQUIRED NOW** |

## Actual blockers
1. **Docker daemon is not running → Paper 1 execution (X1-A/B/C, sandbox attacks, fault injection needing Docker) is BLOCKED** until the user starts Docker Desktop and the sandbox image is confirmed. It does **not** block the approved Phase-1 items: X3-0, X2-A, the Paper-1 *stored-data* claim/latency analysis.
2. **Zotero library is empty** → no verified reference can be retrieved; not a Phase-1 blocker; blocks any later citation step until the user adds sources.
3. Non-blocking cautions: environment is unlocked (pins violated); W&B credentials exist but were not validated online; Codex CLI could not be confirmed on PATH.

## Tools that will be used in the 5-hour sprint
Serena (navigation), Context7 (API checks), Superpowers (verification discipline), Git (status/hashes/tags), Python `.venv` read-only with safeguards, SB3 + Gymnasium (X3-0c replay only, after the gate/approvals), pytest (only if a dummy manifest-helper check needs it). claude-md-management only with user approval. **Not used:** Docker (blocked; needed only for X1), W&B (disabled), Zotero (empty; not needed), Codex/Antigravity/Gemini/NotebookLM (later, external).

## Not done (by design)
No repair of any tool; no Docker start; no test run; no W&B network call; no Zotero write; no MCP configuration change; no Phase-1 action.
