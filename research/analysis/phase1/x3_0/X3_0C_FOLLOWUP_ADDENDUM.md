# X3-0c follow-up addendum (manual inspection of session logs; DESCRIPTIVE)

Inspection of `x3_0c_replay_session_log.csv` (no new computation): of the 25 PPO+G sessions whose executed difficulty path differs from Constant-Same+G, only 5 differ in session MAE. In the other sessions the two paths differ but the MAE is identical. Example: persona `overconfident_fail` (target 1.5): PPO+G oscillates 1 -> 2 -> 1 -> 2 while Constant-Same+G stays at 1; both are 0.5 away from the target at every turn, so the tracking MAE is the same (0.591) although volatility and oscillation differ. **The tracking-MAE endpoint cannot distinguish trajectories that alternate between the two integer levels adjacent to a half-integer target.** Consequence for the X3-A protocol (recorded, protocol drafts unchanged here): the MAE equivalence test must be read together with the volatility/oscillation endpoints, and the persona generator must state whether targets fall on half-integers (all frozen targets do: 1.0, 1.5, 3.0, 4.0, 4.5; targets 1.5 and 4.5 are equidistant from two integer levels).
