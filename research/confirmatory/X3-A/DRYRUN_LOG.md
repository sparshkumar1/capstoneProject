# X3-A harness dry-run log (non-confirmatory fixture: the 5 frozen personas; no confirmatory data exist)

- **Attempt 1** (`dryrun_attempt1_FAILED_subtle_mutant_undetected/`): gate G-HARNESS failed 8/9. Equivalence checks (Fixed, Heuristic, PPO raw and PPO+G x 5 seeds, Constant-Same+G) matched the frozen `run_session_trajectory` exactly (0 mismatching fields), the shield copy equalled the frozen shield on 200 000 random/edge states, and two mutants (G5 removed; G4 threshold 0.31) were detected. The third mutant (heuristic Harder-threshold 0.76 instead of 0.75) was NOT detected: no turn of the 250 fixture turns had performance in (0.75, 0.76], so the mutant is behaviourally identical on that fixture. Per the sprint rule the run aborted before any result was produced (correct behaviour).
- **Resolution (before any confirmatory data exist; recorded as a harness-design deviation, not a scientific change):** the mutant was too subtle to be a valid positive control on a 250-turn fixture. It is replaced by a behaviourally material mutant (threshold 0.60) and the 0.76 variant is kept as an informational, non-gating row. No equivalence tolerance, criterion or comparison was relaxed.
- **Attempt 2** (`dryrun_attempt2_FAILED_upper_threshold_mutants_undetected/`): with the Harder-threshold changed to 0.60 the mutant was again not detected (0 mismatching fields). Diagnosis: the fixture personas' performance values are essentially never in (0.60, 0.75] at the visited difficulties, so any change to the heuristic's *upper* threshold is behaviourally invisible on this fixture (checked: for `normal`, performance stays at 0.46-0.54). This is a limitation of the fixture for that branch, not evidence that the comparison is insensitive; the equivalence comparison is exact (9 fields per session).
- **Attempt 3 (this version):** the gating heuristic mutant changes the *lower* (Easier) threshold 0.40 -> 0.50, a branch the fixture exercises; the two upper-threshold mutants (0.60, 0.76) are retained as informational, non-gating rows (expected: undetected on this fixture). Recorded before any confirmatory data exist; no equivalence criterion was relaxed. Mutant choice was made by inspecting fixture behaviour only.
